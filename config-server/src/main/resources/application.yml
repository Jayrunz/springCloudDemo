server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jayrunz/SpringCloudConfig/
          search-paths: configDemo
          username:
          password:
      label: master

