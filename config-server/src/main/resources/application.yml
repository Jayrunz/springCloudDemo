server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#          git地址
          uri: https://github.com/Jayrunz/SpringCloudConfig/
#          目录
          search-paths: configDemo
          username:
          password:
#      分支
      label: master

